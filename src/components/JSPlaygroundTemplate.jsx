import React, { Component } from 'react';

class JSPlaygroundTemplate extends Component {

    render() {
        return (
        	<React.Fragment>
	        	look at da code in js playground biatch. 
	        	Grab what you need to then have this call specifics of each challenge either from here or new component files.
        	</React.Fragment>
        );
    }
}

export default JSPlaygroundTemplate;